/**
 * EXPERIMENTAL Organic minimum spacing (Phase 15.5). Deliberately NOT exported
 * from `src/core/index.ts`; the production engine is only called, never changed.
 */
export * from './spacingParameters';
export * from './passSpacing';
export * from './variants';
